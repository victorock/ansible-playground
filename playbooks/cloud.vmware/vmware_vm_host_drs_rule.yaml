---
- name: "module_vmware_vm_host_drs_rule"
  hosts: all
  # hosts: localhost, Define the --limit localhost.
  # connection: Define the variable ansible_connection in the inventory
  # become: Define the variable ansible_become in the inventory
  # gather_facts: Define the variable ansible_gather_facts in the inventory
  vars:
    vmware_vm_host_drs_rule: 
      username: "{{ vmware_vm_host_drs_rule_username | default(omit, True) }}"
      drs_rule_name: "{{ vmware_vm_host_drs_rule_drs_rule_name | default(omit, True) }}"
      hostname: "{{ vmware_vm_host_drs_rule_hostname | default(omit, True) }}"
      mandatory: "{{ vmware_vm_host_drs_rule_mandatory | default(omit, True) }}"
      datacenter: "{{ vmware_vm_host_drs_rule_datacenter | default(omit, True) }}"
      host_group_name: "{{ vmware_vm_host_drs_rule_host_group_name | default(omit, True) }}"
      enabled: "{{ vmware_vm_host_drs_rule_enabled | default(omit, True) }}"
      vm_group_name: "{{ vmware_vm_host_drs_rule_vm_group_name | default(omit, True) }}"
      affinity_rule: "{{ vmware_vm_host_drs_rule_affinity_rule | default(omit, True) }}"
      state: "{{ vmware_vm_host_drs_rule_state | default(omit, True) }}"
      password: "{{ vmware_vm_host_drs_rule_password | default(omit, True) }}"
      validate_certs: "{{ vmware_vm_host_drs_rule_validate_certs | default(omit, True) }}"
      port: "{{ vmware_vm_host_drs_rule_port | default(omit, True) }}"
      cluster_name: "{{ vmware_vm_host_drs_rule_cluster_name | default(omit, True) }}"
  tasks:
    - name: "plugin_vmware_vm_host_drs_rule"
      vmware_vm_host_drs_rule: "{{ vmware_vm_host_drs_rule }}"
      register: r_vmware_vm_host_drs_rule
