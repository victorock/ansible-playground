---
- name: "module_avi_analyticsprofile"
  hosts: all
  # hosts: localhost, Define the --limit localhost.
  # connection: Define the variable ansible_connection in the inventory
  # become: Define the variable ansible_become in the inventory
  # gather_facts: Define the variable ansible_gather_facts in the inventory
  vars:
    avi_analyticsprofile: 
      hs_event_throttle_window: "{{ avi_analyticsprofile_hs_event_throttle_window | default('omit', True) }}"
      apdex_rtt_tolerated_factor: "{{ avi_analyticsprofile_apdex_rtt_tolerated_factor | default('omit', True) }}"
      disable_se_analytics: "{{ avi_analyticsprofile_disable_se_analytics | default('omit', True) }}"
      apdex_server_rtt_tolerated_factor: "{{ avi_analyticsprofile_apdex_server_rtt_tolerated_factor | default('omit', True) }}"
      hs_security_nonpfs_penalty: "{{ avi_analyticsprofile_hs_security_nonpfs_penalty | default('omit', True) }}"
      hs_security_tls12_score: "{{ avi_analyticsprofile_hs_security_tls12_score | default('omit', True) }}"
      exclude_server_tcp_reset_as_error: "{{ avi_analyticsprofile_exclude_server_tcp_reset_as_error | default('omit', True) }}"
      hs_min_dos_rate: "{{ avi_analyticsprofile_hs_min_dos_rate | default('omit', True) }}"
      exclude_no_dns_record_as_error: "{{ avi_analyticsprofile_exclude_no_dns_record_as_error | default('omit', True) }}"
      conn_server_lossy_zero_win_size_event_threshold: "{{ avi_analyticsprofile_conn_server_lossy_zero_win_size_event_threshold | default('omit', True) }}"
      hs_max_resources_penalty: "{{ avi_analyticsprofile_hs_max_resources_penalty | default('omit', True) }}"
      conn_lossy_total_rexmt_threshold: "{{ avi_analyticsprofile_conn_lossy_total_rexmt_threshold | default('omit', True) }}"
      client_log_streaming_config: "{{ avi_analyticsprofile_client_log_streaming_config | default('omit', True) }}"
      hs_pscore_traffic_threshold_l4_client: "{{ avi_analyticsprofile_hs_pscore_traffic_threshold_l4_client | default('omit', True) }}"
      exclude_no_valid_gs_member_as_error: "{{ avi_analyticsprofile_exclude_no_valid_gs_member_as_error | default('omit', True) }}"
      hs_security_encalgo_score_rc4: "{{ avi_analyticsprofile_hs_security_encalgo_score_rc4 | default('omit', True) }}"
      apdex_server_response_threshold: "{{ avi_analyticsprofile_apdex_server_response_threshold | default('omit', True) }}"
      hs_security_cipherscore_ge128b: "{{ avi_analyticsprofile_hs_security_cipherscore_ge128b | default('omit', True) }}"
      resp_code_block: "{{ avi_analyticsprofile_resp_code_block | default('omit', True) }}"
      hs_performance_boost: "{{ avi_analyticsprofile_hs_performance_boost | default('omit', True) }}"
      uuid: "{{ avi_analyticsprofile_uuid | default('omit', True) }}"
      exclude_invalid_dns_domain_as_error: "{{ avi_analyticsprofile_exclude_invalid_dns_domain_as_error | default('omit', True) }}"
      exclude_http_error_codes: "{{ avi_analyticsprofile_exclude_http_error_codes | default('omit', True) }}"
      hs_max_anomaly_penalty: "{{ avi_analyticsprofile_hs_max_anomaly_penalty | default('omit', True) }}"
      exclude_unsupported_dns_query_as_error: "{{ avi_analyticsprofile_exclude_unsupported_dns_query_as_error | default('omit', True) }}"
      state: "{{ avi_analyticsprofile_state | default('omit', True) }}"
      exclude_gs_down_as_error: "{{ avi_analyticsprofile_exclude_gs_down_as_error | default('omit', True) }}"
      apdex_server_response_tolerated_factor: "{{ avi_analyticsprofile_apdex_server_response_tolerated_factor | default('omit', True) }}"
      avi_api_patch_op: "{{ avi_analyticsprofile_avi_api_patch_op | default('omit', True) }}"
      disable_server_analytics: "{{ avi_analyticsprofile_disable_server_analytics | default('omit', True) }}"
      conn_server_lossy_timeo_rexmt_threshold: "{{ avi_analyticsprofile_conn_server_lossy_timeo_rexmt_threshold | default('omit', True) }}"
      exclude_client_close_before_request_as_error: "{{ avi_analyticsprofile_exclude_client_close_before_request_as_error | default('omit', True) }}"
      hs_security_weak_signature_algo_penalty: "{{ avi_analyticsprofile_hs_security_weak_signature_algo_penalty | default('omit', True) }}"
      conn_server_lossy_ooo_threshold: "{{ avi_analyticsprofile_conn_server_lossy_ooo_threshold | default('omit', True) }}"
      hs_security_selfsignedcert_penalty: "{{ avi_analyticsprofile_hs_security_selfsignedcert_penalty | default('omit', True) }}"
      conn_server_lossy_total_rexmt_threshold: "{{ avi_analyticsprofile_conn_server_lossy_total_rexmt_threshold | default('omit', True) }}"
      conn_lossy_timeo_rexmt_threshold: "{{ avi_analyticsprofile_conn_lossy_timeo_rexmt_threshold | default('omit', True) }}"
      api_version: "{{ avi_analyticsprofile_api_version | default('omit', True) }}"
      username: "{{ avi_analyticsprofile_username | default('omit', True) }}"
      hs_security_certscore_gt30d: "{{ avi_analyticsprofile_hs_security_certscore_gt30d | default('omit', True) }}"
      description: "{{ avi_analyticsprofile_description | default('omit', True) }}"
      hs_security_ssl30_score: "{{ avi_analyticsprofile_hs_security_ssl30_score | default('omit', True) }}"
      apdex_rum_tolerated_factor: "{{ avi_analyticsprofile_apdex_rum_tolerated_factor | default('omit', True) }}"
      hs_security_cipherscore_eq000b: "{{ avi_analyticsprofile_hs_security_cipherscore_eq000b | default('omit', True) }}"
      hs_security_certscore_le30d: "{{ avi_analyticsprofile_hs_security_certscore_le30d | default('omit', True) }}"
      exclude_persistence_change_as_error: "{{ avi_analyticsprofile_exclude_persistence_change_as_error | default('omit', True) }}"
      exclude_syn_retransmit_as_error: "{{ avi_analyticsprofile_exclude_syn_retransmit_as_error | default('omit', True) }}"
      ranges: "{{ avi_analyticsprofile_ranges | default('omit', True) }}"
      exclude_dns_policy_drop_as_significant: "{{ avi_analyticsprofile_exclude_dns_policy_drop_as_significant | default('omit', True) }}"
      controller: "{{ avi_analyticsprofile_controller | default('omit', True) }}"
      exclude_invalid_dns_query_as_error: "{{ avi_analyticsprofile_exclude_invalid_dns_query_as_error | default('omit', True) }}"
      apdex_server_rtt_threshold: "{{ avi_analyticsprofile_apdex_server_rtt_threshold | default('omit', True) }}"
      hs_security_hsts_penalty: "{{ avi_analyticsprofile_hs_security_hsts_penalty | default('omit', True) }}"
      exclude_server_dns_error_as_error: "{{ avi_analyticsprofile_exclude_server_dns_error_as_error | default('omit', True) }}"
      hs_security_tls11_score: "{{ avi_analyticsprofile_hs_security_tls11_score | default('omit', True) }}"
      password: "{{ avi_analyticsprofile_password | default('omit', True) }}"
      hs_security_encalgo_score_none: "{{ avi_analyticsprofile_hs_security_encalgo_score_none | default('omit', True) }}"
      avi_api_update_method: "{{ avi_analyticsprofile_avi_api_update_method | default('omit', True) }}"
      tenant: "{{ avi_analyticsprofile_tenant | default('omit', True) }}"
      conn_lossy_zero_win_size_event_threshold: "{{ avi_analyticsprofile_conn_lossy_zero_win_size_event_threshold | default('omit', True) }}"
      apdex_rum_threshold: "{{ avi_analyticsprofile_apdex_rum_threshold | default('omit', True) }}"
      hs_pscore_traffic_threshold_l4_server: "{{ avi_analyticsprofile_hs_pscore_traffic_threshold_l4_server | default('omit', True) }}"
      name: "{{ avi_analyticsprofile_name | default('omit', True) }}"
      hs_max_security_penalty: "{{ avi_analyticsprofile_hs_max_security_penalty | default('omit', True) }}"
      apdex_rtt_threshold: "{{ avi_analyticsprofile_apdex_rtt_threshold | default('omit', True) }}"
      avi_credentials: "{{ avi_analyticsprofile_avi_credentials | default('omit', True) }}"
      tenant_ref: "{{ avi_analyticsprofile_tenant_ref | default('omit', True) }}"
      apdex_response_tolerated_factor: "{{ avi_analyticsprofile_apdex_response_tolerated_factor | default('omit', True) }}"
      exclude_tcp_reset_as_error: "{{ avi_analyticsprofile_exclude_tcp_reset_as_error | default('omit', True) }}"
      hs_security_chain_invalidity_penalty: "{{ avi_analyticsprofile_hs_security_chain_invalidity_penalty | default('omit', True) }}"
      api_context: "{{ avi_analyticsprofile_api_context | default('omit', True) }}"
      client_log_config: "{{ avi_analyticsprofile_client_log_config | default('omit', True) }}"
      conn_lossy_ooo_threshold: "{{ avi_analyticsprofile_conn_lossy_ooo_threshold | default('omit', True) }}"
      hs_security_cipherscore_lt128b: "{{ avi_analyticsprofile_hs_security_cipherscore_lt128b | default('omit', True) }}"
      url: "{{ avi_analyticsprofile_url | default('omit', True) }}"
      hs_security_tls10_score: "{{ avi_analyticsprofile_hs_security_tls10_score | default('omit', True) }}"
      hs_security_certscore_expired: "{{ avi_analyticsprofile_hs_security_certscore_expired | default('omit', True) }}"
      apdex_response_threshold: "{{ avi_analyticsprofile_apdex_response_threshold | default('omit', True) }}"
      tenant_uuid: "{{ avi_analyticsprofile_tenant_uuid | default('omit', True) }}"
      hs_security_certscore_le07d: "{{ avi_analyticsprofile_hs_security_certscore_le07d | default('omit', True) }}"
  tasks:
    - name: "plugin_avi_analyticsprofile"
      avi_analyticsprofile: "{{ avi_analyticsprofile }}"
