---
- name: "module_os_project_facts"
  hosts: all
  # hosts: localhost, Define the --limit localhost.
  # connection: Define the variable ansible_connection in the inventory
  # become: Define the variable ansible_become in the inventory
  # gather_facts: Define the variable ansible_gather_facts in the inventory
  vars:
    os_project_facts: 
      auth_type: "{{ os_project_facts_auth_type | default(omit, True) }}"
      domain: "{{ os_project_facts_domain | default(omit, True) }}"
      region_name: "{{ os_project_facts_region_name | default(omit, True) }}"
      availability_zone: "{{ os_project_facts_availability_zone | default(omit, True) }}"
      verify: "{{ os_project_facts_verify | default(omit, True) }}"
      filters: "{{ os_project_facts_filters | default(omit, True) }}"
      api_timeout: "{{ os_project_facts_api_timeout | default(omit, True) }}"
      auth: "{{ os_project_facts_auth | default(omit, True) }}"
      name: "{{ os_project_facts_name | default(omit, True) }}"
      key: "{{ os_project_facts_key | default(omit, True) }}"
      cacert: "{{ os_project_facts_cacert | default(omit, True) }}"
      cert: "{{ os_project_facts_cert | default(omit, True) }}"
      timeout: "{{ os_project_facts_timeout | default(omit, True) }}"
      interface: "{{ os_project_facts_interface | default(omit, True) }}"
      cloud: "{{ os_project_facts_cloud | default(omit, True) }}"
      wait: "{{ os_project_facts_wait | default(omit, True) }}"
  tasks:
    - name: "plugin_os_project_facts"
      os_project_facts: "{{ os_project_facts }}"
      register: r_os_project_facts
