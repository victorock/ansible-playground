---
- name: "module_docker_swarm_service"
  docker_swarm_service: "{{ docker_swarm_service }}"
  register: r_docker_swarm_service
  vars:
    docker_swarm_service: 
      tty: "{{ docker_swarm_service_tty | default(omit, True) }}"
      restart_policy_window: "{{ docker_swarm_service_restart_policy_window | default(omit, True) }}"
      update_max_failure_ratio: "{{ docker_swarm_service_update_max_failure_ratio | default(omit, True) }}"
      reservations: "{{ docker_swarm_service_reservations | default(omit, True) }}"
      update_monitor: "{{ docker_swarm_service_update_monitor | default(omit, True) }}"
      labels: "{{ docker_swarm_service_labels | default(omit, True) }}"
      container_labels: "{{ docker_swarm_service_container_labels | default(omit, True) }}"
      ssl_version: "{{ docker_swarm_service_ssl_version | default(omit, True) }}"
      docker_host: "{{ docker_swarm_service_docker_host | default(omit, True) }}"
      resolve_image: "{{ docker_swarm_service_resolve_image | default(omit, True) }}"
      reserve_memory: "{{ docker_swarm_service_reserve_memory | default(omit, True) }}"
      rollback_config: "{{ docker_swarm_service_rollback_config | default(omit, True) }}"
      networks: "{{ docker_swarm_service_networks | default(omit, True) }}"
      client_key: "{{ docker_swarm_service_client_key | default(omit, True) }}"
      read_only: "{{ docker_swarm_service_read_only | default(omit, True) }}"
      endpoint_mode: "{{ docker_swarm_service_endpoint_mode | default(omit, True) }}"
      dns_options: "{{ docker_swarm_service_dns_options | default(omit, True) }}"
      mounts: "{{ docker_swarm_service_mounts | default(omit, True) }}"
      log_driver_options: "{{ docker_swarm_service_log_driver_options | default(omit, True) }}"
      hostname: "{{ docker_swarm_service_hostname | default(omit, True) }}"
      working_dir: "{{ docker_swarm_service_working_dir | default(omit, True) }}"
      publish: "{{ docker_swarm_service_publish | default(omit, True) }}"
      update_config: "{{ docker_swarm_service_update_config | default(omit, True) }}"
      state: "{{ docker_swarm_service_state | default(omit, True) }}"
      dns: "{{ docker_swarm_service_dns | default(omit, True) }}"
      command: "{{ docker_swarm_service_command | default(omit, True) }}"
      env: "{{ docker_swarm_service_env | default(omit, True) }}"
      image: "{{ docker_swarm_service_image | default(omit, True) }}"
      api_version: "{{ docker_swarm_service_api_version | default(omit, True) }}"
      restart_config: "{{ docker_swarm_service_restart_config | default(omit, True) }}"
      update_delay: "{{ docker_swarm_service_update_delay | default(omit, True) }}"
      log_driver: "{{ docker_swarm_service_log_driver | default(omit, True) }}"
      ca_cert: "{{ docker_swarm_service_ca_cert | default(omit, True) }}"
      replicas: "{{ docker_swarm_service_replicas | default(omit, True) }}"
      healthcheck: "{{ docker_swarm_service_healthcheck | default(omit, True) }}"
      args: "{{ docker_swarm_service_args | default(omit, True) }}"
      env_files: "{{ docker_swarm_service_env_files | default(omit, True) }}"
      tls_hostname: "{{ docker_swarm_service_tls_hostname | default(omit, True) }}"
      limit_cpu: "{{ docker_swarm_service_limit_cpu | default(omit, True) }}"
      user: "{{ docker_swarm_service_user | default(omit, True) }}"
      groups: "{{ docker_swarm_service_groups | default(omit, True) }}"
      reserve_cpu: "{{ docker_swarm_service_reserve_cpu | default(omit, True) }}"
      placement: "{{ docker_swarm_service_placement | default(omit, True) }}"
      configs: "{{ docker_swarm_service_configs | default(omit, True) }}"
      tls: "{{ docker_swarm_service_tls | default(omit, True) }}"
      limits: "{{ docker_swarm_service_limits | default(omit, True) }}"
      logging: "{{ docker_swarm_service_logging | default(omit, True) }}"
      name: "{{ docker_swarm_service_name | default(omit, True) }}"
      stop_grace_period: "{{ docker_swarm_service_stop_grace_period | default(omit, True) }}"
      limit_memory: "{{ docker_swarm_service_limit_memory | default(omit, True) }}"
      secrets: "{{ docker_swarm_service_secrets | default(omit, True) }}"
      update_parallelism: "{{ docker_swarm_service_update_parallelism | default(omit, True) }}"
      force_update: "{{ docker_swarm_service_force_update | default(omit, True) }}"
      restart_policy: "{{ docker_swarm_service_restart_policy | default(omit, True) }}"
      dns_search: "{{ docker_swarm_service_dns_search | default(omit, True) }}"
      client_cert: "{{ docker_swarm_service_client_cert | default(omit, True) }}"
      update_failure_action: "{{ docker_swarm_service_update_failure_action | default(omit, True) }}"
      stop_signal: "{{ docker_swarm_service_stop_signal | default(omit, True) }}"
      restart_policy_delay: "{{ docker_swarm_service_restart_policy_delay | default(omit, True) }}"
      hosts: "{{ docker_swarm_service_hosts | default(omit, True) }}"
      mode: "{{ docker_swarm_service_mode | default(omit, True) }}"
      timeout: "{{ docker_swarm_service_timeout | default(omit, True) }}"
      debug: "{{ docker_swarm_service_debug | default(omit, True) }}"
      update_order: "{{ docker_swarm_service_update_order | default(omit, True) }}"
      validate_certs: "{{ docker_swarm_service_validate_certs | default(omit, True) }}"
      restart_policy_attempts: "{{ docker_swarm_service_restart_policy_attempts | default(omit, True) }}"
      constraints: "{{ docker_swarm_service_constraints | default(omit, True) }}"

- name: "export r_docker_swarm_service"
  set_stats:
    data:
      r_docker_swarm_service: "{{ r_docker_swarm_service }}"
