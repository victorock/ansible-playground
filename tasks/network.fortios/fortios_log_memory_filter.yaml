---
- name: "module_fortios_log_memory_filter"
  fortios_log_memory_filter: "{{ fortios_log_memory_filter }}"
  register: r_fortios_log_memory_filter
  vars:
    fortios_log_memory_filter: 
      username: "{{ fortios_log_memory_filter_username | default(omit, True) }}"
      host: "{{ fortios_log_memory_filter_host | default(omit, True) }}"
      log_memory_filter: "{{ fortios_log_memory_filter_log_memory_filter | default(omit, True) }}"
      https: "{{ fortios_log_memory_filter_https | default(omit, True) }}"
      password: "{{ fortios_log_memory_filter_password | default(omit, True) }}"
      vdom: "{{ fortios_log_memory_filter_vdom | default(omit, True) }}"
