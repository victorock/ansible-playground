---
- name: "module_ec2_ami_facts"
  ec2_ami_facts: "{{ ec2_ami_facts }}"
  register: r_ec2_ami_facts
  vars:
    ec2_ami_facts: 
      profile: "{{ ec2_ami_facts_profile | default(omit, True) }}"
      aws_secret_key: "{{ ec2_ami_facts_aws_secret_key | default(omit, True) }}"
      aws_access_key: "{{ ec2_ami_facts_aws_access_key | default(omit, True) }}"
      security_token: "{{ ec2_ami_facts_security_token | default(omit, True) }}"
      executable_users: "{{ ec2_ami_facts_executable_users | default(omit, True) }}"
      region: "{{ ec2_ami_facts_region | default(omit, True) }}"
      owners: "{{ ec2_ami_facts_owners | default(omit, True) }}"
      image_ids: "{{ ec2_ami_facts_image_ids | default(omit, True) }}"
      describe_image_attributes: "{{ ec2_ami_facts_describe_image_attributes | default(omit, True) }}"
      filters: "{{ ec2_ami_facts_filters | default(omit, True) }}"
      ec2_url: "{{ ec2_ami_facts_ec2_url | default(omit, True) }}"
      validate_certs: "{{ ec2_ami_facts_validate_certs | default(omit, True) }}"
